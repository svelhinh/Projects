Si on observe le code du fichier level04.pl, on remarque qu'on peut se connecter en localhost sur le port 4747 et qu'on peut passer le parametre "x" a l'url.
On remarque egalement qu'on peut executer des commandes sur le serveur et afficher le resultat de ces commandes grace a la ligne "print `echo $y 2>&1`;".
On envoie donc en parametre un \n encode, donc %0A, pour ignorer la variable $y et getflag pour obtenir le flag.
